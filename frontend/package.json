{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 30080",
    "build": "tsc && vite build",
    "preview": "vite preview --port 30080",
    "serve": "vite preview --host --port 30080"
  },
  "devDependencies": {
    "ts-proto": "^1.148.0",
    "typescript": "^5.0.2",
    "vite": "^4.3.2"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.8.14"
  }
}